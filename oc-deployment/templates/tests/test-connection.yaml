apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "oc-deployment.fullname" . }}-test-connection"
  labels:
    {{- include "oc-deployment.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "oc-deployment.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
